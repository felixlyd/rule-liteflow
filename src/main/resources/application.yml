spring:
  application:
    name: rule-liteflow

server:
  port: 6666

liteflow:
  rule-source: config/flow.el.xml

# eureka配置信息
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/

